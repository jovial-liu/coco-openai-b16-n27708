# Figure contract and provenance

Figure 1: explain frozen candidates, selection-only target-tolerant reranking,
and the fixed-region boundary before held-out audit. This is a protocol diagram,
not measured response traces. Main-text equations and experimental results are unchanged.
Current visual design restores the scatter/epsilon-feasible-region version;
the subsequent bar-based redesign was rejected by the user. Eight candidate
masks, a one-way fixed-region boundary and three held-out templates remain.
Small vector glyphs show a lock, a bullseye, a descending response curve
and a patch-grid localization box. These glyphs do not encode new observations.
The reranking arrow is gently curved; all three horizontal process arrows
share FLOW_Y=77. The boundary label uses title case to improve clearance.
The candidate-response geometry is illustrative, explicitly identified as such
in the caption. Ordinary labels are >=9 pt at the final 178 mm inclusion width.

Figure 2: compare normalized aggregate gains across four settings and three
evaluation protocols. Use four small-multiple interval plots with a common zero
and x scale. Twelve paired estimates and their archived paired 95% CIs are read
without rounding or recalculation. Full / cross-foil / absent-only use circle /
square / triangle as well as blue / orange / neutral; color is not the only cue.
Output: single-column, 86 mm wide, editable-text SVG and vector PDF; inspect at
print size. No axis breaks, no decorative gradients, no rasterized chart layers.

Exact data source: ICASSP2027_SUBMISSION_COMPLIANCE_FINAL.zip,
source/data/round2_identification.csv, copied unchanged to data/.
SHA256: ad81b2cf0531fda2bc408998af25bc72928bd0f96525a043174e6ad57eb63743.
All Absent-only endpoints come from this archived CSV, not visual inference.
The source contains more digits than the manuscript's rounded presentation.

The supplied qualitative_cases_compact_labels(1).png is included unchanged.
IEEEtran.bst is the unmodified Michael Shell v1.12 distribution file obtained from
https://github.com/suixiaodan/IEEE-BibTeX-style/blob/master/IEEEtran.bst.
It supplies a missing build dependency. The authorized author revision adds
ye2026idea to references.bib; all prior bibliography entries are retained.

The delivered Figure 1 is generated by `python draw_figure1.py`; Figure 3 by
`python draw_figure3.py`. `python make_figures.py` invokes these dedicated
scripts and generates Figure 2. Ordinary figure labels are at least 9 pt;
mathematical superscripts/subscripts retain conventional reduced sizes.
Then run pdflatex, bibtex, pdflatex, pdflatex on main.tex.
