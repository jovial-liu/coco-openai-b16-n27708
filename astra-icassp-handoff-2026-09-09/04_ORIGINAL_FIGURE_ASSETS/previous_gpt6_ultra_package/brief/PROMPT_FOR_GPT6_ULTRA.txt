Use the repository files as the authoritative scientific/design context. Redesign Figure 1 as one compact IEEE/ICASSP-ready all-vector two-panel figure. Panel (a), ~60-62% width, should show Original CCI -> epsilon=0.02 target-tolerant feasible set -> worst-foil rerank -> REGION FIXED -> held-out audit, with a visually explicit selection/evaluation boundary and no held-out feedback into selection. Keep only essential equations and the three separately reported endpoints: specificity, held-out target response, and locality. Panel (b), ~38-40% width, should replace the old large vertical error-bar chart with a compact horizontal forest plot for COCO B/16, COCO B/32, VOC B/16, VOC B/32 and Full/Cross-foil/Absent-only series. Use the exact data CSV; do not invent missing absent-only CI endpoints. Make it grayscale-readable via marker shapes, full vector, white background, no gradients/shadows, restrained rounded corners, 0.75-0.85 pt main strokes, final text >=6.5-7 pt, total height about 47-50 mm. Avoid the schematic wording `Negative -> Less negative`; use neutral wording or omit that micro-panel. Deliver editable SVG + vector PDF and ensure no overlap/clipping/rasterized text.
